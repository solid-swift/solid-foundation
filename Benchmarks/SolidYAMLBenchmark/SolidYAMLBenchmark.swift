// Benchmark boilerplate generated by Benchmark

import Benchmark
import Foundation
import SolidData
import SolidIO
import SolidYAML


let benchmarks: @Sendable () -> Void = {

  var config = Benchmark.Configuration()
  config.scalingFactor = .one

  // MARK: - Fixtures (Basic)

  let smallMap = Value.object([
    .string("a"): .number(1),
    .string("b"): .bool(true),
    .string("c"): .string("hello"),
  ])

  let smallArray = Value.array([
    .number(1), .number(2), .number(3), .string("x"), .bool(false)
  ])

  let largeArray: Value = .array((0..<10_000).map { .number($0) })

  let largeMap: Value = .object(
    (0..<5_000).reduce(into: Value.Object()) { dict, idx in
      dict[.string("k\(idx)")] = .number(idx)
    }
  )

  let largeString = String(repeating: "abcdefgh", count: 2048)

  // MARK: - Fixtures (YAML-Specific)

  let multilineString = Value.string(
    "First line\n  indented line\n\nblank line above\ntrailing newline\n"
  )

  // 20-level deep nested maps: {a: {b: {c: ... }}}
  let deeplyNested: Value = {
    var value: Value = .string("leaf")
    for i in stride(from: 19, through: 0, by: -1) {
      value = .object([.string("level\(i)"): value])
    }
    return value
  }()

  let taggedValue: Value = .tagged(tags: [.string("custom")], value: .string("data"))

  // Pre-built YAML string with anchors & aliases (cannot be represented as Value directly)
  let anchoredYaml = Data("""
    shared: &shared
      x: 1
      y: 2
    ref1: *shared
    ref2: *shared
    ref3: *shared
    """.utf8)

  // Multi-document YAML string
  let multiDocYaml = Data("""
    ---
    name: doc1
    value: 1
    ---
    name: doc2
    value: 2
    ---
    name: doc3
    value: 3
    ...
    """.utf8)

  // MARK: - Pre-encoded Data

  let smallMapYaml = try? YAMLValueWriter.write(smallMap)
  let smallArrayYaml = try? YAMLValueWriter.write(smallArray)
  let largeArrayYaml = try? YAMLValueWriter.write(largeArray)
  let largeMapYaml = try? YAMLValueWriter.write(largeMap)
  let largeStringYaml = try? YAMLValueWriter.write(.string(largeString))
  let multilineStringYaml = try? YAMLValueWriter.write(multilineString)
  let deeplyNestedYaml = try? YAMLValueWriter.write(deeplyNested)
  let taggedYaml = try? YAMLValueWriter.write(taggedValue)

  // MARK: - Value Encode

  Benchmark("YAML Encode Small Map", configuration: config) { benchmark in
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      let data = try? YAMLValueWriter.write(smallMap)
      blackHole(data)
    }
  }

  Benchmark("YAML Encode Small Array", configuration: config) { benchmark in
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      let data = try? YAMLValueWriter.write(smallArray)
      blackHole(data)
    }
  }

  Benchmark("YAML Encode Large Array", configuration: config) { benchmark in
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      let data = try? YAMLValueWriter.write(largeArray)
      blackHole(data)
    }
  }

  Benchmark("YAML Encode Large Map", configuration: config) { benchmark in
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      let data = try? YAMLValueWriter.write(largeMap)
      blackHole(data)
    }
  }

  Benchmark("YAML Encode Large String", configuration: config) { benchmark in
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      let data = try? YAMLValueWriter.write(.string(largeString))
      blackHole(data)
    }
  }

  Benchmark("YAML Encode Multiline String", configuration: config) { benchmark in
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      let data = try? YAMLValueWriter.write(multilineString)
      blackHole(data)
    }
  }

  Benchmark("YAML Encode Deeply Nested", configuration: config) { benchmark in
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      let data = try? YAMLValueWriter.write(deeplyNested)
      blackHole(data)
    }
  }

  Benchmark("YAML Encode Tagged", configuration: config) { benchmark in
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      let data = try? YAMLValueWriter.write(taggedValue)
      blackHole(data)
    }
  }

  // MARK: - Value Decode

  Benchmark("YAML Decode Small Map", configuration: config) { benchmark in
    guard let smallMapYaml else { return }
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      var reader = try YAMLValueReader(data: smallMapYaml)
      let value = try? reader.read()
      blackHole(value)
    }
  }

  Benchmark("YAML Decode Small Array", configuration: config) { benchmark in
    guard let smallArrayYaml else { return }
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      var reader = try YAMLValueReader(data: smallArrayYaml)
      let value = try? reader.read()
      blackHole(value)
    }
  }

  Benchmark("YAML Decode Large Array", configuration: config) { benchmark in
    guard let largeArrayYaml else { return }
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      var reader = try YAMLValueReader(data: largeArrayYaml)
      let value = try? reader.read()
      blackHole(value)
    }
  }

  Benchmark("YAML Decode Large Map", configuration: config) { benchmark in
    guard let largeMapYaml else { return }
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      var reader = try YAMLValueReader(data: largeMapYaml)
      let value = try? reader.read()
      blackHole(value)
    }
  }

  Benchmark("YAML Decode Large String", configuration: config) { benchmark in
    guard let largeStringYaml else { return }
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      var reader = try YAMLValueReader(data: largeStringYaml)
      let value = try? reader.read()
      blackHole(value)
    }
  }

  Benchmark("YAML Decode Multiline String", configuration: config) { benchmark in
    guard let multilineStringYaml else { return }
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      var reader = try YAMLValueReader(data: multilineStringYaml)
      let value = try? reader.read()
      blackHole(value)
    }
  }

  Benchmark("YAML Decode Deeply Nested", configuration: config) { benchmark in
    guard let deeplyNestedYaml else { return }
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      var reader = try YAMLValueReader(data: deeplyNestedYaml)
      let value = try? reader.read()
      blackHole(value)
    }
  }

  Benchmark("YAML Decode Tagged", configuration: config) { benchmark in
    guard let taggedYaml else { return }
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      var reader = try YAMLValueReader(data: taggedYaml)
      let value = try? reader.read()
      blackHole(value)
    }
  }

  Benchmark("YAML Decode Anchors & Aliases", configuration: config) { benchmark in
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      var reader = try YAMLValueReader(data: anchoredYaml)
      let value = try? reader.read()
      blackHole(value)
    }
  }

  // MARK: - Stream Writer

  Benchmark("YAML Stream Writer Small Map", configuration: config) { benchmark in
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      let sink = DataSink()
      let writer = YAMLStreamWriter(sink: sink)
      let events = EmitEventEncoder().encode(smallMap)
      for event in events {
        try? await writer.write(event)
      }
      try? await writer.finish()
      blackHole(sink.data)
    }
  }

  Benchmark("YAML Stream Writer Force Block", configuration: config) { benchmark in
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      let sink = DataSink()
      let writer = YAMLStreamWriter(sink: sink, options: .init(forceBlockCollections: true))
      let events = EmitEventEncoder().encode(smallMap)
      for event in events {
        try? await writer.write(event)
      }
      try? await writer.finish()
      blackHole(sink.data)
    }
  }

  // MARK: - Stream Reader

  Benchmark("YAML Stream Reader Small Map", configuration: config) { benchmark in
    guard let smallMapYaml else { return }
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      let source = DataSource(data: smallMapYaml)
      let reader = YAMLStreamReader()
      let driver = FormatStreamReaderDriver(reader: reader, source: source, bufferSize: 64)
      var decoder = ParseEventDecoder(resolver: YAMLScalarResolver())
      while let event = try? await driver.next() {
        try? decoder.append(event)
      }
      let value = try? decoder.finish()
      blackHole(value)
    }
  }

  Benchmark("YAML Stream Reader Chunked", configuration: config) { benchmark in
    guard let smallMapYaml else { return }
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      let source = ChunkedSource(data: smallMapYaml, chunkSizes: [1, 2, 3, 1, 4])
      let reader = YAMLStreamReader()
      let driver = FormatStreamReaderDriver(reader: reader, source: source, bufferSize: 64)
      var decoder = ParseEventDecoder(resolver: YAMLScalarResolver())
      while let event = try? await driver.next() {
        try? decoder.append(event)
      }
      let value = try? decoder.finish()
      blackHole(value)
    }
  }

  // MARK: - Document Reader (Multi-Doc)

  Benchmark("YAML Document Reader Multi-Doc", configuration: config) { benchmark in
    benchmark.startMeasurement()
    for _ in benchmark.scaledIterations {
      let reader = try YAMLDocumentReader(data: multiDocYaml)
      let docs = try? reader.readAll()
      blackHole(docs)
    }
  }
}

// MARK: - Helpers

private final class ChunkedSource: Source, @unchecked Sendable {
  private var data: Data
  private let chunkSizes: [Int]
  private var chunkIndex = 0
  private var closed = false
  private var bytesReadValue = 0

  init(data: Data, chunkSizes: [Int]) {
    self.data = data
    self.chunkSizes = chunkSizes
  }

  var bytesRead: Int {
    get async throws { bytesReadValue }
  }

  func read(max: Int) async throws -> Data? {
    guard !closed else { throw IOError.streamClosed }
    guard !data.isEmpty else { return nil }

    let requested =
      chunkSizes.isEmpty
      ? max
      : chunkSizes[min(chunkIndex, chunkSizes.count - 1)]
    chunkIndex += 1

    let size = min(max, requested, data.count)
    let result = data.prefix(size)
    data.removeSubrange(0..<result.count)
    bytesReadValue += result.count
    return result
  }

  func close() async throws {
    closed = true
  }
}
